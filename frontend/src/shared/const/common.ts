export enum AccessLevels {
  User = 'user',
  Admin = 'admin',
}

export enum InternalRoutes {
  Admin = '/admin/',
  Login = '/login/',
  Logout = '/logout/',
  Main = '/',
}

export const emptyObject = {};
